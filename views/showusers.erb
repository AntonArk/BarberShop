<table class="table table-striped">
    <thead>
        <tr>
            <th>№</th>
            <th>Имя</th>
            <th>Телефон</th>
            <th>Дата записи</th>
            <th>Барбер</th>
            <th>Цвет</th>
        </tr>
    </thead>

    <body>
        <% @results.each do |row| %>
            <tr>
                <td><%= row['id'] %></td>
                <td><%= row['username'] %></td>
                <td><%= row['phone'] %></td>
                <td><%= row['date_stamp'] %></td>
                <td><%= row['barber']%></td>
                <td><%= row['colour'] %></td>
            </tr>
      
        <% end %>
  </body>
  
</table>